
spring.application.name=rendezvous-service
# Port du serveur
server.port=8005

# Configuration Mail (exemple pour Gmail)
# Configuration SMTP Sécurisée
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hervekarlmarx@gmail.com
spring.mail.password=sxdp yzcw rmyk cqwn  
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Database (adaptez pour la prod)
spring.datasource.url=jdbc:mysql://localhost:3306/rdvbd
spring.datasource.username=root
spring.datasource.password=root

# Activer le debug
logging.level.org.springframework=DEBUG
logging.level.org.hibernate=INFO
logging.level.com.yourpackage=TRACE

# Désactiver la mise à jour automatique du schéma en prod
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA
# spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Configuration d'Eureka (si utilisé)
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.prefer-ip-address=true


# # Configuration de logging
# logging.level.org.springframework=INFO
# logging.level.com.yourcompany=DEBUG

patient.service.url=http://localhost:8000
personnel.service.url=http://localhost:8001
